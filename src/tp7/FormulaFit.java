package tp7;

import ch.unige.ng.fitness.FormulaFitness;
import ch.unige.ng.species.Evaluable;
import ch.unige.ng.species.stackGP.operations.Operation;

/**
 * Created by thomas on 29.12.14.
 */
public class FormulaFit extends FormulaFitness {

	@Override
	public double compute(Evaluable<Operation> operations) {
		return super.compute(operations);
	}
}
